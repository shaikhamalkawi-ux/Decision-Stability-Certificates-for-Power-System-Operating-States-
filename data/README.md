# Data

Store only redistributable admitted data here.

For external datasets that should not be redistributed, store acquisition instructions, source identifiers, versions, query parameters, checksums, and provenance instead of copying restricted data.
